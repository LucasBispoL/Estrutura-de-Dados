"# Estrutura-de-Dados" 

Isso é um teste de como funciona o Git e GitHub.
Estou colocando algumas implementações que fiz durante a matéria de 
Algoritmos e Estrutura de Dados.

-- Problemas para resolver:
In file included from main.cpp:3:0:
heap.h: In member function 'void heap::heapify(int)':
heap.h:33:27: error: lvalue required as unary '&' operand
             int e = &Esq(i);
                           ^
heap.h:34:27: error: lvalue required as unary '&' operand
             int d = &Dir(i);
                           ^
